rule all:
	input:
		expand("fastqc_analysis/{sample}_fastqc.zip", sample=config["samples"]),
		expand("fastqc_analysis/{sample}_fastqc.html", sample=config["samples"]),
		expand("fastqc_analysis/{sample}_fastqc.zip", sample=config["controls"]),
		expand("fastqc_analysis/{sample}_fastqc.html", sample=config["controls"]),
		expand("fastqc_analysis/{sample}.sorted.dedup_fastqc.zip", sample=config["samples"]),
		expand("fastqc_analysis/{sample}.sorted.dedup_fastqc.html", sample=config["samples"]),
		expand("fastqc_analysis/{sample}.sorted.dedup_fastqc.zip", sample=config["controls"]),
		expand("fastqc_analysis/{sample}.sorted.dedup_fastqc.html", sample=config["controls"]),

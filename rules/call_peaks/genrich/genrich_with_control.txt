# expand("genrich_peaks/{sample}_{control}_peak.narrowPeak", sample=config["sample_groups"], control=config["control_groups"])

rule name_sort_for_genrich:
	input:
		"bam_files/{sample}.sorted.dedup.bam"
	output:
		"bam_files/{sample}.namesorted_for_genrich.bam"
	shell:
		"samtools sort -n -@ THREADS -O BAM {input} > {output}"

def get_sample_groups(wildcards):
	if len(wildcards.sample.split('_')) == 1:
		return "bam_files/{sample}.namesorted_for_genrich.bam"
	else:
		exp = wildcards.sample.split('_')
		return expand("bam_files/{sample}.namesorted_for_genrich.bam", sample=exp)

def get_control_groups(wildcards):
	if len(wildcards.control.split('_')) == 1:
		return "bam_files/{control}.namesorted_for_genrich.bam"
	else:
		exp = wildcards.control.split('_')
		return expand("bam_files/{control}.namesorted_for_genrich.bam", sample=exp)

rule call_peaks:
	input:
		s = get_sample_groups,
		c = get_control_groups
	output:
		peak   = "genrich_peaks/{sample}_{control}_peak.narrowPeak",
		reads  = "genrich_peaks/{sample}_{control}_reads.bed",
		pileup = "genrich_peaks/{sample}_{control}_pileup"
	run:
		if type(input.s) is snakemake.io.Namedlist:
			sample_input = ','.join(input.s)
		else:
			sample_input = input.s
		if type(input.c) is snakemake.io.Namedlist:
			control_input = ','.join(input.c)
		else:
			control_input = input.c
		shell("Genrich -t {sample_input} -c {control_input} "
			"-o {output.peak} -f {output.pileup} -b {output.reads} READ_TYPE")

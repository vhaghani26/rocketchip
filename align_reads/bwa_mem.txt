# Assuming that the genome has already been indexed

rule align_reads:
	input:
		genome = multiext("data/GENOME_PREFIX", ".amb", ".ann", ".bwt", ".pac", ".sa"),
		r1 = "data/samples/{sample}_1.fastq",
		r2 = "data/samples/{sample}_2.fastq"
	output:
		"aligned_reads/{sample}.sam"
	shell:
		"bwa mem data/GENOME_PREFIX {input.r1} {input.r2} > {output}"
